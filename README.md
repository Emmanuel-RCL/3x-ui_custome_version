#🚀 راهنمای نصب X-UI با انتخاب نسخه دلخواه

این اسکریپت به شما اجازه می‌دهد نسخه مورد نظر خود از پنل مدیریت X-UI را مستقیماً از مخزن رسمی GitHub نصب کنید.
با این روش می‌توانید بدون نیاز به دامنه و تنها با IP سرور، پنل را در چند ثانیه راه‌اندازی کنید.

🧩 دستور نصب سریع

برای اجرای اسکریپت کافی است دستور زیر را در ترمینال سرور خود وارد کنید:

bash <(curl -Ls https://raw.githubusercontent.com/Emmanuel-RCL/3x-ui_custome_version/main/install.sh)

⚙️ مراحل نصب

پس از اجرای دستور بالا، اسکریپت از شما شماره نسخه x-ui را می‌پرسد.

نسخه باید به فرمت x.y.z وارد شود (مثلاً 2.6.3 یا 2.5.8)

مثال:

🔢 Enter desired version (e.g., 2.6.3): 2.6.3


اسکریپت به‌صورت خودکار فایل مورد نظر را از لینک رسمی زیر دانلود می‌کند:

https://github.com/MHSanaei/3x-ui/releases/download/vx.y.z/x-ui-linux-amd64.tar.gz


فایل فشرده استخراج شده و در مسیر زیر نصب یا به‌روزرسانی می‌شود:

/usr/local/x-ui


سرویس X-UI به‌صورت خودکار فعال و راه‌اندازی می‌شود.

🌐 دسترسی به پنل

پس از نصب موفق، می‌توانید با IP سرور و پورت پیش‌فرض وارد پنل شوید:

http://<YOUR_SERVER_IP>:54321


به‌عنوان مثال:

http://192.168.1.10:54321


نام کاربری و رمز عبور پیش‌فرض:

Username: admin
Password: admin


برای تغییر رمز یا پورت، می‌توانید از دستور زیر استفاده کنید:

x-ui

🛠️ ویژگی‌های این اسکریپت

🔹 پشتیبانی از نصب هر نسخه دلخواه (x.y.z)

🔹 دریافت مستقیم از GitHub رسمی MHSanaei

🔹 بدون نیاز به دامنه (قابل دسترسی با IP)

🔹 اجرای خودکار سرویس و فعال‌سازی در بوت

🔹 به‌روزرسانی آسان نسخه‌های قبلی بدون حذف تنظیمات

⚡ حذف کامل X-UI

در صورت نیاز به حذف کامل، دستور زیر را اجرا کنید:

systemctl stop x-ui
systemctl disable x-ui
rm -rf /usr/local/x-ui /etc/systemd/system/x-ui.service /usr/bin/x-ui
systemctl daemon-reload

📋 نکات مهم

این اسکریپت فقط برای سیستم‌های Linux 64-bit (amd64) طراحی شده است.

برای اجرا، باید کاربر root باشید یا از sudo استفاده کنید.

در صورت خطای دانلود، اطمینان حاصل کنید که نسخه واردشده در GitHub وجود دارد.

🧠 نمونه خروجی پس از نصب
======================================================
✅ X-UI v2.6.3 installed and running successfully!
🌐 Access panel via: http://123.45.67.89:54321
🧩 Default username: admin
🔑 Default password: admin
⚙️  To manage panel, run: x-ui
======================================================
