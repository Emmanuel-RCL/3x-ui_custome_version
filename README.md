# 🚀 نصب‌کننده نسخه دلخواه X-UI

این اسکریپت به شما اجازه می‌دهد **هر نسخه دلخواه از پنل X-UI** را مستقیماً از مخزن رسمی [MHSanaei/3x-ui](https://github.com/MHSanaei/3x-ui) دانلود و نصب کنید.  
همچنین پنل به‌صورت خودکار طوری پیکربندی می‌شود که **بدون نیاز به دامنه و فقط با IP سرور (در حالت HTTP)** در دسترس باشد.

---

## 🧩 دستور نصب سریع

کافی است دستور زیر را در ترمینال سرور خود وارد کنید 👇

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Emmanuel-RCL/3x-ui_custome_version/main/install.sh)
```

---
⚙️ مراحل نصب

پس از اجرای دستور بالا، اسکریپت از شما شماره نسخه مورد نظر را می‌پرسد.
نسخه باید به فرمت x.y.z وارد شود (مانند 2.6.3 یا 2.5.8).

مثال:

🔢 Enter desired version (e.g., 2.6.3): 2.6.3

---
🌐 دسترسی به پنل

پس از نصب موفق، می‌توانید با IP سرور و پورت پیش‌فرض 2053 وارد پنل شوید:
```bash
http://<SERVER-IP>:2053
```
🔑 اطلاعات ورود پیش‌فرض:
Username: admin
Password: admin

برای مدیریت یا تغییر اطلاعات ورود و پورت، از دستور زیر استفاده کنید:
```bash
x-ui
```
---

🛠️ ویژگی‌ها

🔹 امکان نصب هر نسخه دلخواه 

🔹 دریافت مستقیم از مخزن رسمی GitHub

🔹 بدون نیاز به دامنه (دسترسی از طریق IP)

🔹 اجرای خودکار سرویس و فعال‌سازی در بوت سیستم

🔹 امکان به‌روزرسانی بدون حذف تنظیمات قبلی

---
⚡ حذف کامل X-UI

برای حذف کامل پنل از سیستم، از دستورات زیر استفاده کنید:
```bash
systemctl stop x-ui
systemctl disable x-ui
rm -rf /usr/local/x-ui /etc/systemd/system/x-ui.service /usr/bin/x-ui
systemctl daemon-reload
```
---

⚠️ نکات مهم

این اسکریپت فقط برای سیستم‌های Linux 64-bit (amd64) طراحی شده است.

باید با کاربر root اجرا شود (یا از sudo استفاده کنید).

اطمینان حاصل کنید که نسخه واردشده واقعاً در صفحه Releases
 موجود باشد.
 ---

🧠 نمونه خروجی پس از نصب موفق
```
======================================================
✅ X-UI v2.6.3 installed and running successfully!
🌐 Access panel via: http://123.45.67.89:2053
🧩 Default username: admin
🔑 Default password: admin
⚙️  To manage panel, run: x-ui
======================================================
```
